// Worker navbar & sidebar

export default function WorkerLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      {/* TODO: Add worker navigation */}
      {children}
    </div>
  );
}
