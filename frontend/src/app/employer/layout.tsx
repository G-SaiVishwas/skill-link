// Employer navbar & sidebar

export default function EmployerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div>
      {/* TODO: Add employer navigation */}
      {children}
    </div>
  )
}
